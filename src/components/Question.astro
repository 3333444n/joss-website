---
// This seems redundant with FAQ.astro, typically FAQ implies Question & Answer.
// For now, making it a distinct visual style or purpose if needed.
// If it's the same as FAQ.astro, consider removing it.
interface Props {
  questionText: string;
}
const { questionText } = Astro.props;
---
<div class="my-4 p-4 border border-purple-500 rounded-md">
  <h3 class="text-xl font-semibold text-purple-400">{questionText}</h3>
  <div class="text-gray-300 mt-2">
    <slot name="answer" />
  </div>
</div>
